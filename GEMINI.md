# GEMINI.md

Gemini CLI reads this file for project context. Everything you need is in
`AGENTS.md` — imported below so it's always in your context.

@AGENTS.md

## Reminder for every task

1. Read `docs/CONTEXT-MAP.md`, then the `docs/**/CONTEXT.md` for the area you're
   touching, **before** editing.
2. Run `npm run build` before you consider a task done — it must pass.
3. If your change makes a `CONTEXT.md` wrong, fix that doc in the same change.
